[pytest]
markers =
    integration: marks tests as integration tests that use real Neo4j connections
    unit: marks tests as unit tests
    slow: marks tests as slow (taking more than 1s)
    api: marks tests for API endpoints

pythonpath = .
